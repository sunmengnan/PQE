name: Build macOS PQE Dashboard

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r mac_package/requirements-mac.txt

      - name: Build app with PyInstaller
        working-directory: mac_package
        run: |
          pyinstaller --clean --noconfirm PQE_Dashboard.spec
          PQE_DASHBOARD_SMOKE_TEST=1 "dist/PQE Dashboard.app/Contents/MacOS/PQE Dashboard"
          mkdir -p release
          cp -R "dist/PQE Dashboard.app" release/
          cd release
          zip -qry "PQE_Dashboard_macOS_Standalone.zip" "PQE Dashboard.app"

      - name: Upload macOS app artifact
        uses: actions/upload-artifact@v4
        with:
          name: PQE_Dashboard_macOS_Standalone
          path: mac_package/release/PQE_Dashboard_macOS_Standalone.zip
          if-no-files-found: error
